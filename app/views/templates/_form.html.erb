<%= form_for(@template) do |f| %>
  <% if @template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@template.errors.count, "error") %> prohibited this template from being saved:</h2>

      <ul>
      <% @template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :名前 %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :メールアドレス %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :内容 %><br>
    <%= f.text_area :text %>
    <!--<text_area rows="100" cols="100"></text_area>-->
  </div>
  <div class="actions">
    <%= f.submit :送信 %>
  </div>
<% end %>
